# Backlog — Umbrella

Short strategic queue. 1-3 lines per item. When an item grows into multi-section
work, promote it to `ROADMAP.md` and remove the stub here. Strike or remove items
when closed.

- [ ] Save the `agentmemory-src/` local patch as a `.patch` file for clean re-apply after an upstream `git pull` (see ADR-0002).
- [ ] Decide whether `AGENTMEMORY_TOOLS` should escalate from `core` to `all` (and document the trigger).
- [ ] <next strategic item>
